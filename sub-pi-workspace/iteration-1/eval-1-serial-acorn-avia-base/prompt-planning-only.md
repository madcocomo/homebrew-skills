我准备改 AH 里的一个链路：先在 acorn 里调整 Folder 查询逻辑，确认 mvn install 通过后，再去 avia-base 改调用方。请不要一上来就在主线程里读很多代码，我更希望你把局部分析和验证都下沉到独立 pi 进程，并且前一步过了就自动推进下一步。

补充约束：这次评测只需要输出编排方案、任务文件结构、session/输出文件设计和主线程回收策略；不要真的启动任何子进程、tmux、命令或代码修改。